// This file is auto-generated by tsqx. Do not edit manually.
import type { Client, Pool, QueryResult } from "pg";
import { type Users } from "../../generated";

export interface GetUserParams {
  id: number;
}

export const GetUserParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GetUserParams",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
} as const;

export async function GetUser(client: Client | Pool, params: GetUserParams): Promise<Users | null> {
  const sql = 'SELECT * FROM users WHERE id = $1;';
  const result = await client.query(sql, [params.id]);
  return result.rows[0] ?? null;
}
