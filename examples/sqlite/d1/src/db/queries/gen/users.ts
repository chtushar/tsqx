// This file is auto-generated by tsqx. Do not edit manually.
// D1Database type is available globally in Cloudflare Workers

import { type Users, type Posts } from "../../generated";

export interface GetUserParams {
  id: number;
}

export const GetUserParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GetUserParams",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
} as const;

export async function GetUser(db: D1Database, params: GetUserParams): Promise<Users | null> {
  const sql = 'SELECT * FROM users WHERE id = $1;';
  const result = await db.prepare(sql).bind(params.id).first<unknown>();
  return result ?? null;
}

export async function ListUsers(db: D1Database): Promise<Users[]> {
  const sql = 'SELECT * FROM users ORDER BY name;';
  const result = await db.prepare(sql).all();
  return result.results;
}

export interface CreateUserParams {
  name: string;
  email: string;
}

export const CreateUserParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CreateUserParams",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "email"
  ],
  "additionalProperties": false
} as const;

export async function CreateUser(db: D1Database, params: CreateUserParams): Promise<Users | null> {
  const sql = 'INSERT INTO users (name, email) VALUES ($1, $2) RETURNING *;';
  const result = await db.prepare(sql).bind(params.name, params.email).first<unknown>();
  return result ?? null;
}

export interface UpdateUserParams {
  name: string;
  email: string;
  id: number;
}

export const UpdateUserParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UpdateUserParams",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string"
    },
    "id": {
      "type": "integer"
    }
  },
  "required": [
    "name",
    "email",
    "id"
  ],
  "additionalProperties": false
} as const;

export async function UpdateUser(db: D1Database, params: UpdateUserParams): Promise<void> {
  const sql = 'UPDATE users SET name = $1, email = $2, updated_at = datetime(\'now\') WHERE id = $3;';
  await db.prepare(sql).bind(params.name, params.email, params.id).run();
}

export interface DeleteUserParams {
  id: number;
}

export const DeleteUserParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DeleteUserParams",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
} as const;

export async function DeleteUser(db: D1Database, params: DeleteUserParams): Promise<void> {
  const sql = 'DELETE FROM users WHERE id = $1;';
  await db.prepare(sql).bind(params.id).run();
}

export async function ListPublishedPosts(db: D1Database): Promise<Posts[]> {
  const sql = 'SELECT * FROM posts WHERE published = 1 ORDER BY created_at DESC;';
  const result = await db.prepare(sql).all();
  return result.results;
}

export interface CreatePostParams {
  title: string;
  body: string;
  author_id: number;
}

export const CreatePostParamsSchema = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CreatePostParams",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "author_id": {
      "type": "integer"
    }
  },
  "required": [
    "title",
    "body",
    "author_id"
  ],
  "additionalProperties": false
} as const;

export async function CreatePost(db: D1Database, params: CreatePostParams): Promise<Posts | null> {
  const sql = 'INSERT INTO posts (title, body, author_id) VALUES ($1, $2, $3) RETURNING *;';
  const result = await db.prepare(sql).bind(params.title, params.body, params.author_id).first<unknown>();
  return result ?? null;
}
